name: Deploy Website PMR

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Run CI/CD
        run: echo "CI/CD website PMR berjalan sukses!"
